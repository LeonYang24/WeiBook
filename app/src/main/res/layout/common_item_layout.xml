<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/iv__item_icon"
        android:background="@mipmap/discover_default_icon"
        android:layout_width="30dp"
        android:layout_height="30dp"/>

    <ImageView
        android:id="@+id/iv_item_msgtips"
        android:layout_width="7dp"
        android:layout_height="7dp"
        android:layout_gravity="top|right"
        android:layout_marginTop="5dp"
        android:contentDescription="@string/profile_settings"
        android:src="@mipmap/common_msg_tips"
        android:visibility="invisible"/>


    <TextView
        android:id="@+id/tv_item_name"
        android:layout_toRightOf="@+id/iv__item_icon"
        android:layout_marginLeft="15dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:text="111"/>

</LinearLayout>